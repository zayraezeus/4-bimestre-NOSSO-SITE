// Função para simular o envio de mensagem
function enviarMensagem() {
    alert("Mensagem enviada com sucesso!");
    // Evita que o formulário seja realmente enviado (no exemplo)
    event.preventDefault();
  }
  
